# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in mcoin.gemspec
gemspec

gem 'pry'

gem 'rake', '~> 12.0'
gem 'rspec', '~> 3.0'
gem 'simplecov'

gem 'rubocop', '~> 1.9.1'
gem 'rubocop-rake'
gem 'rubocop-rspec'

gem 'bundler-audit'
gem 'overcommit'
